# MongoDB connection string (used by the server)
MONGODB_URI=mongodb://localhost:27017/invizio

# Comma-separated allowed origins for CORS (server). Add your Vercel frontend URL in production.
# Example: CORS_ORIGIN=https://your-app.vercel.app
# CORS_ORIGIN=

# API base URL for the frontend. Set in Vercel (or other host) to your deployed API URL in production.
VITE_API_URL=http://localhost:3000
